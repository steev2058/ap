<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:id="@+id/group_statement">
        <item
            android:id="@+id/menu_statement"
            android:icon="@drawable/ic_accounts"
            android:iconTint="@color/colorPrimary"
            android:title="@string/mini_statement" />
    </group>

    <group android:id="@+id/group_transfer">
        <item
            android:id="@+id/menu_transfer"
            android:icon="@drawable/ic_transfer"
            android:title="@string/transfer" />
    </group>
</menu>